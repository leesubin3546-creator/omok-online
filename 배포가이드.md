# 오목 온라인 - 배포 가이드

## 로컬 실행 (테스트용)

```bash
cd omok
npm install
npm start
# → http://localhost:3000 에서 실행
```

---

## 🚀 Render.com 배포 (무료, 링크 공유 가능)

### 1. GitHub에 올리기
1. GitHub 계정 생성 → 새 레포지토리 생성 (예: `omok-online`)
2. `omok` 폴더 안의 파일 3개를 업로드:
   - `server.js`
   - `package.json`
   - `public/index.html`

### 2. Render.com 연결
1. https://render.com 접속 → 무료 계정 생성
2. **New +** → **Web Service**
3. GitHub 레포지토리 연결
4. 설정:
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. **Create Web Service** 클릭

### 3. 배포 완료
- 약 2~3분 후 `https://omok-online-xxxx.onrender.com` 형태의 링크 생성
- 이 링크를 친구에게 공유하면 바로 플레이 가능!

> ⚠️ 무료 플랜은 15분 비활성 시 슬립 모드 진입 (첫 접속 시 30초 대기)
> 계속 켜두려면 UptimeRobot 같은 서비스로 5분마다 핑을 보내면 됩니다.

---

## 대안: Railway.app

1. https://railway.app 접속
2. **Deploy from GitHub** 선택
3. 같은 방법으로 배포
4. 무료 크레딧 월 $5 제공 (소규모 사용에 충분)
